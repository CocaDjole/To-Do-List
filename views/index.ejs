
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>To-Do List</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <div class="container">
  <h1>To-Do List</h1>
  <form action="/" method="POST">
    <input type="text" name="task" placeholder="Enter task">
    <button type="submit">Add Task</button>
  </form>
  <ul>
    <% tasks.forEach((task, index) => { %>
      <!-- Ovo je komentar u EJS šablonu -->
      <li class="<%= task.completed ? 'completed' : '' %>">
        <form action="/complete/<%= index %>" method="POST">
          <input type="checkbox" <%= task.completed ? 'checked' : '' %> onchange="this.form.submit()">
          <%= task.description %>
        </form>
      </li>
    <% }) %>
  </ul>
  <!--<ul>
    <% tasks.forEach((task, index) => { %>
      <li>
        <form action="/complete/<%= index %>" method="POST">
          <input style= "completed"type="checkbox" <%= task.completed ? 'checked' : '' %> onchange="this.form.submit()">
          <%= task.description %>
        </form>
      </li>
    <% }) %>-->
  </ul>
</div>
</body>
</html>